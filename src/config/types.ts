export enum RateLimitByType {
  USER = 0,
  IP = 1,
}

export enum EnvType {
  DEV = 'dev',
  PRODUCTION = 'production',
  TEST = 'test',
}

export enum PassportAuthType {
  FACEBOOK = 'facebook-token',
}
